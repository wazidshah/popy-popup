# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.swp
*.swo
*~

# Dependency directories (if you ever add a build step)
node_modules/
vendor/

# Build artifacts
*.zip
*.tar.gz
/build/
/dist/

# Local env / secrets
.env
.env.local

# WordPress.org SVN checkout — never nest this inside the git repo,
# but ignore it here as a safety net in case it ever gets checked out
# in the same parent directory by mistake.
svn-popy-popup/
*-svn/

# Log files
*.log
npm-debug.log*

# Local WordPress dev environment configs (if testing via wp-env, Local, etc.)
.wp-env.json
wp-content/
